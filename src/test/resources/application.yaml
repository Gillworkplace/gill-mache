server:
  port: 8160
  tomcat:
    accesslog:
      enabled: true
      pattern: '%t %h %a -> %A %S %H "%r" len: %b resp: %s cost: %D thread: %I'
    basedir: ./
  servlet:
    context-path: /mache
spring:
  application:
    name: gill-mache

logging:
  config: config/log4j2.xml